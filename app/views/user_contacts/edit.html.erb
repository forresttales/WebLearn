<% @title = "user_contacts edit" %>

<h1>user_contacts edit</h1>

<div class="row">
  <div class="span6 offset3">

  	<%= form_for(:user_contact, :url => {:action => 'update', :id => @user_contact.id}) do |f| %>

    <%= render(:partial => 'form', :locals => {:f => f} ) %>

    <div class="form-buttons">
      <%= submit_tag('Submit') %>
    </div>
  
  	<% end %>
  	
  </div>  
</div>
